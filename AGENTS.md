# Historical repository

This repository is retired and has no executable workflow. Do not recreate its private dispatcher, Herdr guard, model routing, prompt bridge, or installer. Refer users to the official version-matched skill emitted by `herdr --skill`.
